
{
  "name": "clouderp-pos",
  "version": "1.0.0",
  "description": "Professional Cloud-Based ERP & POS System",
  "scripts": {
    "start": "serve .",
    "build": "rm -rf dist && mkdir -p dist && cp -rv *.html *.tsx *.ts *.json *.css dist/ 2>/dev/null || :",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
